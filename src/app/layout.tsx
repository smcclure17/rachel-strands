import type { Metadata } from "next";
import "./globals.css";
import { nytFont } from "./fonts";

export const metadata: Metadata = {
  title: "Beautiful Woman Strands",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${nytFont.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
